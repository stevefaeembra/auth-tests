import { Result } from 'postcss';
import { describe, expect, it } from 'vitest';
import { renderHook, waitFor } from '~/test/utils';
import { useLogout } from './logout.hook';

describe('useLogoutTokenHook', () => {
  it.todo('should throw an error if the logout fails', async () => {});

  it.todo('should reset state and redirect to login if logout successful', async () => {});
});
